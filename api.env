OPENAI_API_KEY=your_key_here
CLAUDE_API_KEY=your_key_here
LLAMA_API_KEY=your_key_here
MISTRAL_API_KEY=your_key_here


///Important: If you already committed a file (like .env) before adding it to .gitignore, Git will still track it. You need to remove it first:
git rm --cached .env
git commit -m "Stop tracking .env"
///
